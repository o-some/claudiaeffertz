<section class="ch-card" data-ch-loyalty-status aria-labelledby="ChLoyaltyHeading"><h2 id="ChLoyaltyHeading">{{ heading | default: 'Deine Vorteile' | escape }}</h2><p>{{ explanation | default: 'Punktestand und Vorteile werden sicher durch den verbundenen Anbieter bestätigt.' | escape }}</p><div data-ch-loyalty-provider-state aria-live="polite">{{ unavailable_label | default: 'Status derzeit nicht verbunden' | escape }}</div>{% if terms_url != blank %}<a href="{{ terms_url }}">{{ terms_label | default: 'Bedingungen ansehen' | escape }}</a>{% endif %}</section>
